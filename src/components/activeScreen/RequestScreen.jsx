import { View, Text } from "react-native";
import React from "react";

const RequestScreen = ({items}) => {
  return (
    <View className="flex-1 bg-gray-600">
      <Text>RequestScreen</Text>
    </View>
  );
};

export default RequestScreen;
